version: "3.9"
networks:
  milestoneNet: {}
services:
  php-container:
    build:
      context: ./phpDockerfile
      dockerfile:  Dockerfile
    depends_on:
      - mysql-container
    volumes:
      - ./phpDockerfile:/var/www/html/
    ports:
      - 8000:80
    networks:
      - milestoneNet
  mysql-container:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 2000
      MYSQL_DATABASE: milestone1db
    volumes:
       - ./db-data/milestone1db.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - milestoneNet
  phpmyadmin:
    depends_on:
        - mysql-container
    image: 'phpmyadmin:latest'
    ports:
       - "8060:80"
    networks:
       - milestoneNet
